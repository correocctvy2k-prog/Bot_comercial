version: '3.8'

services:
  # ==========================================
  # MICROSERVICIO 1: COMERCIAL (Bot Principal)
  # ==========================================
  comercial-bot:
    build:
      context: .
    container_name: comercial-bot
    restart: always
    env_file:
      - .env
    ports:
      - "3001:3001"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - TZ=America/Bogota
    command: npm start

  comercial-worker:
    build:
      context: .
    container_name: comercial-worker
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - TZ=America/Bogota
    command: node src/worker.js

  # ==========================================
  # MICROSERVICIO 2: ASAMBLEA (Eventos)
  # ==========================================
  asamblea-bot:
    build:
      context: ./Asamblea
    container_name: asamblea-bot
    restart: always
    env_file:
      - ./Asamblea/.env
    ports:
      - "3002:3002"
    volumes:
      - ./Asamblea:/app
      - /app/node_modules
    environment:
      - TZ=America/Bogota
    command: npm start

  asamblea-worker:
    build:
      context: ./Asamblea
    container_name: asamblea-worker
    restart: always
    env_file:
      - ./Asamblea/.env
    volumes:
      - ./Asamblea:/app
      - /app/node_modules
    environment:
      - TZ=America/Bogota
    command: node src/worker.js

  # ==========================================
  # MICROSERVICIO 3: CRM FRONTEND (Vite/React)
  # ==========================================
  crm-frontend:
    build:
      context: ./CRM_Frontend
    container_name: crm-frontend
    restart: always
    ports:
      - "3003:80"
    environment:
      - TZ=America/Bogota
