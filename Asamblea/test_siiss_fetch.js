const { getSiissToken } = require('./src/services/api.asamblea.service.js'); async function run() { try { const token = await getSiissToken(); const now = new Date(); const yyyy = now.getFullYear(); const mm = String(now.getMonth()+1).padStart(2,'0'); const dd = String(now.getDate()).padStart(2,'0'); const asisfechStr = yyyy + '/' + mm + '/' + dd + ' 00:00:00'; const asishoraStr = yyyy + '/' + mm + '/' + dd + ' 12:00:00'; const body = { accicodi: { accicodi: 1113536281, accirepr: 'JOAN URBANO HERNANDEZ' }, asacodi: 10, asisfech: asisfechStr, asishora: asishoraStr, asisdigi: '123123123', asisause: 0 }; console.log('Sending:', typeof body.accicodi.accicodi, body); const res = await fetch('http://10.192.168.8:8101/siiss-quorum/api/v1/qoAsistencias/registraAsistencia', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + token }, body: JSON.stringify(body) }); console.log('Status:', res.status); const text = await res.text(); console.log('Response body:', text); } catch (err) { console.error('Error:', err); } } run();
